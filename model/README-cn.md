# RAECM - 学生模型蒸馏

**学生模型组件**是RAECM框架中用于高吞吐量路由器属性识别的部分。本模块将教师端LLM流水线的知识蒸馏到高效的任务特定模型中，在实现86.43%准确率的同时支持可扩展部署。

## 📦 模块概述

学生模型组件解决了互联网规模识别中吞吐量与可靠性的权衡问题：

- **高吞吐量**：本地推理，无需API依赖
- **成本效益**：消除按请求计费的API成本
- **强劲准确率**：在基准数据集上保持强劲性能
- **可扩展部署**：适用于持续监测和大规模分析

## 🎯 核心特性

### 知识蒸馏

- **基于证据的训练**：从教师的结构化输出和显式证据中学习
- **任务特定优化**：针对厂商/操作系统/设备类型识别的专用模型
- **检索增强**：保持RAG能力以处理长尾案例
- **独立部署**：可独立运行，无需教师模型依赖

### 模型架构

- **基础模型**：Qwen2.5/Qwen3/Llama3系列
- **LoRA微调**：参数高效的适配
- **多任务支持**：厂商、操作系统和设备类型识别
- **可配置规模**：从1.5B到8B参数

### 部署策略

- **独立推理**：学生模型独立处理识别任务
- **批处理**：高效并行推理
- **质量控制**：置信度评估和结果验证
- **大规模部署**：适用于持续监测场景

## 🚀 快速开始

### 前置要求

- Python 3.8+
- PyTorch 2.0+
- CUDA 11.8+（GPU训练）
- 8GB+ GPU显存（取决于模型大小）

### 安装

```bash
# 安装依赖
pip install -r requirements.txt
```

主要依赖：
- torch >= 2.0.0
- transformers >= 4.30.0
- datasets
- accelerate
- peft
- bitsandbytes

### 下载预训练模型

```bash
# 使用ModelScope（国内推荐）
pip install modelscope
modelscope download --model Qwen/Qwen2.5-3B-Instruct --local_dir ./models/Qwen2.5-3B-Instruct

# 或使用Hugging Face
huggingface-cli download Qwen/Qwen2.5-3B-Instruct --local-dir ./models/Qwen2.5-3B-Instruct
```

### 训练

```bash
# 训练厂商识别模型（推荐：Qwen2.5-3B）
python train.py --mt vd --model qwen2.5-3b

# 训练操作系统识别模型
python train.py --mt os --model qwen2.5-3b

# 训练设备类型识别模型
python train.py --mt dt --model qwen2.5-3b

# 高级训练选项
python train.py --mt vd --model qwen2.5-3b \
  --epochs 3 \
  --batch-size 16 \
  --learning-rate 2e-5 \
  --max-length 2048
```

### 评估

```bash
# 评估训练好的模型
python evaluate.py --mt vd

# 指定模型路径
python evaluate.py --mt vd --model output/vendor/Qwen2.5-3B/best_model

# 快速评估（50个样本）
python evaluate.py --mt vd --max-samples 50
```

### 预测

```bash
# 单个预测
python predict.py --mt vd \
  --model output/vendor/Qwen2.5-3B/best_model \
  --input "Port 8291 (Winbox), HTTP banner: RouterOS"

# 批量预测
python predict.py --mt vd \
  --model output/vendor/Qwen2.5-3B/best_model \
  --input-file test_data.jsonl \
  --output-file predictions.jsonl
```

## 📁 项目结构

### 核心文件

```
model/
├── train.py                     # 训练入口
├── evaluate.py                  # 评估入口
├── predict.py                   # 预测入口
├── full_run.py                  # 完整流水线（训练+评估）
├── config.yaml                  # 全局配置
├── requirements.txt             # 依赖项
├── README.md                    # 英文文档
└── README-cn.md                 # 中文文档（本文件）
```

### 训练核心

```
model/training/
├── config.py                    # 配置管理
├── data_processor.py            # 数据处理流水线
├── trainer.py                   # 标准训练器
├── evaluator.py                 # 模型评估
├── inference.py                 # 推理服务
├── simple_classifier.py         # 简化分类训练
├── train_evaluator.py           # 训练评估器
├── metrics_recorder.py          # 指标记录
├── model_manager.py             # 模型管理
├── model_presets.py             # 模型预设
├── gpu_check.py                 # GPU检查
├── data_pipeline_v2.py          # 数据流水线v2
├── evaluation_v2.py             # 评估v2
│
└── distillation/                # 蒸馏训练模块
    ├── config.py               # 蒸馏配置
    ├── trainer.py              # 蒸馏训练器
    ├── datasets.py             # 数据集处理
    ├── losses.py               # 损失函数
    ├── schedulers.py           # 学习率调度器
    ├── memory.py               # 内存管理
    └── utils.py                # 工具函数
```

### 模型配置

```
model/configs/
├── Qwen2.5-1.5B-Instruct.yaml   # Qwen2.5-1.5B配置
├── Qwen2.5-3B-Instruct.yaml     # Qwen2.5-3B配置（推荐）
├── Qwen2.5-7B-Instruct.yaml     # Qwen2.5-7B配置
├── Qwen3-0.6B.yaml              # Qwen3-0.6B配置
├── Qwen3-4B.yaml                # Qwen3-4B配置
├── Qwen3-8B-Base.yaml           # Qwen3-8B配置
├── Llama-3-8B-Instruct.yaml     # Llama3-8B配置
└── ...                          # 其他模型配置
```

### 训练数据

```
model/input/
├── example_train.jsonl          # 示例训练数据
├── vendor_model_train.jsonl     # 厂商识别数据
├── os_model_train.jsonl         # 操作系统识别数据
└── devicetype_model_train.jsonl # 设备类型识别数据
```

### 提示词模板

```
model/prompt/
├── product_prompts.json         # 产品识别提示词
├── check_prompts.json           # 验证提示词
├── student.json                 # 学生模型提示词
└── new_prompt.json              # 新提示词
```

### 训练脚本

```
model/bash/
├── qwen3-0.6b.sh                # Qwen3-0.6B训练脚本
├── qwen3-8b.sh                  # Qwen3-8B训练脚本
├── qwen3-32b.sh                 # Qwen3-32B训练脚本
└── llama3-8b.sh                 # Llama3-8B训练脚本
```

### 运行时目录

```
model/
├── models/                      # 预训练模型（需下载）
│   ├── Qwen2.5-3B-Instruct/
│   └── ...
│
├── output/                      # 训练输出
│   ├── vendor/                 # 厂商识别模型
│   ├── os/                     # 操作系统识别模型
│   └── devicetype/             # 设备类型识别模型
│
├── result/                      # 评估结果
│   ├── evaluation_report_vd.json
│   ├── evaluation_report_vd.md
│   └── ...
│
└── logs/                        # 训练日志
```

## 🎯 任务类型

| 参数 | 任务 | 描述 | 示例标签 |
|------|------|------|----------|
| `vd` | 厂商识别 | 识别设备厂商 | MikroTik、Cisco、Juniper |
| `os` | 操作系统识别 | 识别操作系统 | RouterOS、IOS、JunOS |
| `dt` | 设备类型 | 识别设备类型 | router、switch、firewall |

## 💻 推荐模型配置

### Qwen2.5系列（推荐）⭐

| 模型 | 参数量 | 显存 | 速度 | 使用场景 |
|------|--------|------|------|----------|
| qwen2.5-1.5b | 1.5B | ~8GB | 最快 | 快速测试 |
| qwen2.5-3b | 3B | ~12GB | 快 | **生产环境（推荐）** |
| qwen2.5-7b | 7B | ~20GB | 中等 | 高准确率需求 |

### Qwen3系列

| 模型 | 参数量 | 显存 | 速度 | 使用场景 |
|------|--------|------|------|----------|
| qwen3-0.6b | 0.6B | ~6GB | 最快 | 快速原型 |
| qwen3-4b | 4B | ~14GB | 快 | 平衡性能 |
| qwen3-8b | 8B | ~22GB | 中等 | 高准确率 |

### Llama3系列

| 模型 | 参数量 | 显存 | 速度 | 使用场景 |
|------|--------|------|------|----------|
| llama3-8b | 8B | ~22GB | 中等 | 对比实验 |

## 📊 训练参数

### 基础配置

```bash
python train.py \
  --mt vd \                    # 任务类型（vd/os/dt）
  --model qwen2.5-3b \         # 模型名称
  --epochs 3 \                 # 训练轮数
  --batch-size 16 \            # 批大小
  --learning-rate 2e-5 \       # 学习率
  --max-length 2048            # 最大序列长度
```

### 高级配置

```bash
python train.py \
  --mt vd \
  --model qwen2.5-3b \
  --lora-r 8 \                 # LoRA秩
  --lora-alpha 16 \            # LoRA alpha
  --lora-dropout 0.05 \        # LoRA dropout
  --warmup-ratio 0.1 \         # 预热比例
  --weight-decay 0.01 \        # 权重衰减
  --gradient-accumulation 4    # 梯度累积步数
```

## 📈 评估

### 基础评估

```bash
# 评估厂商识别模型
python evaluate.py --mt vd

# 评估操作系统识别模型
python evaluate.py --mt os

# 评估设备类型识别模型
python evaluate.py --mt dt
```

### 指定模型路径

```bash
python evaluate.py \
  --mt vd \
  --model output/vendor/Qwen2.5-3B/best_model
```

### 评估输出

生成综合报告：
- `result/evaluation_report_vd.json` - JSON格式详细报告
- `result/evaluation_report_vd.md` - Markdown格式报告

报告包含：
- 总体指标（准确率、F1分数）
- 每类指标（精确率、召回率、F1）
- 混淆矩阵
- 标签分布
- 错误分析

## 🔮 模型预测

### 单个预测

```bash
python predict.py \
  --mt vd \
  --model output/vendor/Qwen2.5-3B/best_model \
  --input "Port 8291 (Winbox), HTTP banner: RouterOS"
```

### 批量预测

```bash
python predict.py \
  --mt vd \
  --model output/vendor/Qwen2.5-3B/best_model \
  --input-file test_data.jsonl \
  --output-file predictions.jsonl
```

## 🎛️ 配置文件

### 全局配置（config.yaml）

```yaml
# 训练配置
training:
  epochs: 3
  batch_size: 16
  learning_rate: 2e-5
  max_length: 2048
  
# LoRA配置
lora:
  r: 8
  alpha: 16
  dropout: 0.05
  
# 数据配置
data:
  train_ratio: 0.8
  valid_ratio: 0.1
  test_ratio: 0.1
```

### 模型配置（configs/*.yaml）

每个模型都有自己的配置文件，包含：
- 模型路径
- 分词器配置
- 训练参数
- LoRA参数

## 🚀 完整训练流水线

使用 `full_run.py` 进行端到端的训练-评估-预测：

```bash
python full_run.py --mt vd --model qwen2.5-3b
```

流水线包括：
1. 数据预处理
2. 模型训练
3. 模型评估
4. 报告生成
5. 最佳模型保存

## 📊 数据格式

### 训练数据（JSONL）

```json
{
  "input": "Port 8291 (Winbox), HTTP banner: RouterOS v6.49",
  "output": "MikroTik",
  "Services": [...]
}
```

### 预测输入（JSONL）

```json
{
  "IP Index": "192.168.1.1",
  "Services": [
    {
      "Port": 8291,
      "Protocol": "Winbox",
      "Banner": "RouterOS v6.49"
    }
  ]
}
```

### 预测输出（JSONL）

```json
{
  "IP Index": "192.168.1.1",
  "Vendor": "MikroTik",
  "Confidence": 0.95,
  "Services": [...]
}
```

## 📊 数据处理流水线

```
标注数据（input/*.jsonl）
    ↓
数据处理（data_processor.py）
    ├─ 格式转换
    ├─ 训练/验证/测试划分
    └─ 分词
    ↓
模型训练（trainer.py）
    ├─ LoRA微调
    ├─ 梯度累积
    └─ 检查点保存
    ↓
模型评估（evaluator.py）
    ├─ 准确率计算
    ├─ 每类指标
    └─ 混淆矩阵
    ↓
训练好的模型（output/*/best_model）
```

## 🎯 使用场景

### 场景1：高吞吐量识别

```
扫描数据 → 学生模型 → 快速分类
```

1. 部署训练好的学生模型
2. 批量处理观测数据
3. 在保持准确率的同时实现高吞吐量

### 场景2：持续监测

```
定期扫描 → 学生模型 → 纵向分析
```

1. 部署学生模型进行持续监测
2. 跟踪属性随时间的变化
3. 成本效益的大规模分析

## ❓ 常见问题

### Q1：需要多少训练数据？

推荐数据量：
- **最少**：每类足够的样本以确保模型收敛
- **推荐**：每类充足的样本以获得稳健性能
- **理想**：每类丰富的多样化样本数据集

### Q2：训练需要多长时间？

训练时间取决于：
- 模型大小（较小模型训练更快）
- 数据量（更多数据需要更长训练时间）
- GPU性能（更好的GPU缩短训练时间）

### Q3：如何处理GPU显存不足？

解决方案：
- 减小 `batch_size`（如从16减到8）
- 减小 `max_length`（如从2048减到1024）
- 使用更小的模型（如qwen2.5-1.5b）
- 启用梯度累积

### Q4：如何提高模型准确率？

策略：
- 增加训练数据
- 增加训练轮数
- 调整学习率
- 使用更大的模型
- 调优LoRA参数

### Q5：如何优化推理速度？

优化方法：
- 使用更小的模型（qwen2.5-1.5b）
- 增加批大小
- 使用GPU推理
- 启用混合精度（fp16/bf16）

### Q6：准确率-成本权衡是什么？

| 策略 | 准确率 | 成本 | 吞吐量 |
|------|--------|------|--------|
| 教师 | 最高 | 高 | 低 |
| 学生 | 强劲 | 低 | 高 |

### Q7：学生模型与教师流水线的关系？

学生模型通过知识蒸馏从教师流水线学习：
- 兼容的输入/输出格式
- 可独立部署运行
- 共享的任务定义和标签体系
- 一致的评估指标

## 🔬 性能指标

基于基准评估：

| 指标 | 教师 | 学生 |
|------|------|------|
| 准确率 | 最高 | 强劲 |
| 吞吐量 | 低 | 高 |
| 成本 | 高 | 低 |
| 延迟 | 高 | 低 |

## 💡 最佳实践

### 模型选择

- **快速测试**：qwen2.5-1.5b或qwen3-0.6b
- **生产部署**：qwen2.5-3b（推荐）
- **高准确率需求**：qwen2.5-7b或qwen3-8b

### 训练参数

- **学习率**：通常在1e-5到2e-5范围内
- **批大小**：从16开始（显存允许时增加）
- **训练轮数**：通常3-5轮（根据验证性能调整）

### 数据准备

- 确保数据质量（清洁、去重）
- 平衡类别分布
- 合理划分训练/验证/测试集

### 评估指标

- 关注F1分数（平衡精确率和召回率）
- 检查混淆矩阵（识别易混淆类别）
- 分析错误样本（改进数据或模型）

## 📚 相关文档

- [training/config.py](training/config.py) - 训练配置
- [training/trainer.py](training/trainer.py) - 训练器实现
- [training/evaluator.py](training/evaluator.py) - 评估器实现
- [../README-cn.md](../README-cn.md) - 项目总览
- [../recog/README-cn.md](../recog/README-cn.md) - 教师流水线


